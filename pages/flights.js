import LandingContext from "../context/landing-page"
import Layout from "../layout"

const Flights = ()=>{
    return(
        <LandingContext>
            <Layout>
              This is flight page
            </Layout>
        </LandingContext>
    )
}

export default Flights